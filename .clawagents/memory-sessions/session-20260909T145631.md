## Task
hi

## Outcome
done

## Result
Ready. Ready to complete the voice assistant and book RAG system in MUBSIR.

Plan:
1. Complete Python package setup (`faster-whisper`, `sounddevice`, `edge-tts`, `pymupdf`, `sentence-transformers`, `faiss-cpu` / cosine vector index).
2. Refactor `assistant.py` into clean modular Python architecture:
   - STT (`faster-whisper`, CUDA float16 with fallback, silence/VAD detection).
   - LLM (`qwen3-fast:latest` via Ollama, reasoning-leak protection `

